[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
kernel_api = { path = "../kernel_api" }
limine = { version = "0.2.0", features = ["uuid"] }
spin = "0.9.8"
uuid = { version = "1.9.1", default-features = false }
